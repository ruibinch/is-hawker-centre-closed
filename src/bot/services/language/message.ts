import { t } from '../../../lang';

export function makeLanguageUpdatedMessage(): string {
  return t('language.updated');
}

export function makeErrorUpdatingLanguageMessage(): string {
  return t('language.error-updating');
}
